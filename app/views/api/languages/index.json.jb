@languages.map do |language|
  {
    message: "Would you like to know more about this one?",
    id: language.id,
    name: language.name,
    desc: language.desc,
    rarity: language.rarity,
    typical_speakers: language.speakers,
    script: language.script,
  }
end

# json.array! @alignments, partial: "alignment", as: :alignment