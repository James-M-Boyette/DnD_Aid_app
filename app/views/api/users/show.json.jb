# json.partial! @alignment, partial: "alignment", as: :alignment

{
  message: "Here's what we know about this user:",
  id: @user.id,
  # May not need user ID
  # userid: "user"params[:id].to_string,
  user_id: @user.user_id,
  email: @user.email,
  ufirstname: @user.ufirstname,
  ulastname: @user.ulastname,
  # username: @user.username,
  password: @user.password,
  password_confirmation: @user.password_confirmation,
}
