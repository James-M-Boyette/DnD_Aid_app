# json.partial! @alignment, partial: "alignment", as: :alignment

{
  message: "You wanna see *this* alignment?",
  id: @alignment.id,
  name: @alignment.name,
  abbreviation: @alignment.abbreviation,
  desc: @alignment.desc,
  imgurl: @alignment.imgurl,
}
